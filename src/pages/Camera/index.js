import React, { useEffect, useState } from 'react';
import { View, Text } from 'react-native';

import {
  Container,
  Post,
  Header,
  Avatar,
  Name,
  Description,
  Loading,
} from './styles';

const Camera = () => {
  return (
    <View>
      <Text>Camera aki</Text>
    </View>
  );
};

export default Camera;
